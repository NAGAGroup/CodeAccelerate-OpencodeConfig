You are re-establishing context after compression.

Use the skill tool to load the following-plans skill.

Use the skill tool to load the sequential-thinking skill.

Use the qdrant_qdrant-find tool to retrieve accumulated session findings using {{PLAN_NAME}} as the collection_name. Consider what has been discovered so far, what decisions have been made, and what constraints must carry forward.

Use the sequential-thinking_sequentialthinking tool to synthesize the retrieved context and reorient yourself. Consider what has been accomplished, what remains to be done, and how the next step builds on the work so far.

After all four tools have been called, use the next_step tool to advance to the next execution step.

**Constraints:** This step is reorientation only. Load both skills fully and retrieve comprehensive context before advancing.
