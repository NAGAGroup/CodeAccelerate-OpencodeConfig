If you haven't already, load the tailwrench-delegation skill and the sequential-thinking skill before doing anything else.

You are staging and committing changes at a meaningful save point.

Use the sequential-thinking_sequentialthinking tool to compose the commit message and scope.

Consider what changes were made since the last commit, whether the project is in a stable committable state, and what the commit message should describe.

Use the task tool to dispatch @tailwrench to stage and commit the changes with an appropriate message.

Include what to commit, what should be excluded, and what the commit message should convey.

After tailwrench completes the commit, use the next_step tool to advance to the next step.

**Constraints:** Only commit when the project is in a stable state.

Exclude secrets and credentials from commits.

Write a commit message that accurately reflects what changed.
