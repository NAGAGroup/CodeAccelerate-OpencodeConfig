You are executing a plan.

This step ends the session successfully.

**Todo List (do these in order):**
1. Call the `write` tool to write a success summary to `{{SESSION_PATH}}/notes/success-summary.md`.
2. Call `next_step` to end the session.

**Rules:**
- Write a clear summary of what was accomplished.
- Include what was changed, verified, and committed.
- Note anything the user should be aware of going forward.
