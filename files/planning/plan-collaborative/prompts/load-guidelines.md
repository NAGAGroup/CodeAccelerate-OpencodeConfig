# Node: load-guidelines — /plan-collaborative

The plan design guidelines have been loaded into your context from `plan-design-guidelines.md`.

Internalize the schema and planning best-practices before proceeding. Pay particular attention to:

- How to structure `plan.json` nodes correctly
- The role of `session-overview` as the mandatory entry node
- How to write `session-overview.md` dynamically (not verbatim) in `finalize`
- Gate node branching conventions

## Advance

Call `next_step()` to proceed.
