---
description: "Start a generic planning session — explore, design, decompose, and write a project DAG"
---

You are starting a generic planning session. The user's topic or description is: `$ARGUMENTS`

> ⚠️ **MANDATORY EXECUTION PROTOCOL — NOT OPTIONAL**
>
> You MUST call `plan_generic()` right now to activate the planning DAG. Do not proceed with any other work until you have called this tool. The tool will inject the first step prompt into the conversation.

Call `plan_generic()` immediately.
