---
description: "Start a debug planning session — bug intake, context gathering, hypothesis formation, session plan creation"
---

You are starting a debug planning session. The bug description or context is: `$ARGUMENTS`

> ⚠️ **MANDATORY EXECUTION PROTOCOL — NOT OPTIONAL**
>
> You MUST call `plan_debug()` right now to activate the debug planning DAG. Do not proceed with any other work until you have called this tool. The tool will inject the first step prompt into the conversation.

Call `plan_debug()` immediately.
