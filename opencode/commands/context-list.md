---
description: "List all persistent context files (project-local and global)."
agent: headwrench
---

List all context files:

**Project-local** (`.opencode/context/`):
- Show filename, first line (title), and file size

**Global** (`~/.config/opencode/context/`):
- Show filename, first line (title), and file size

If either location is empty or doesn't exist, say so.
