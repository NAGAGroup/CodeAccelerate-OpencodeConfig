# Node: load-guidelines — /plan-debug

The plan design guidelines have been loaded into context from `plan-design-guidelines.md`.

Internalize the schema and planning best-practices before proceeding. They will govern how you structure the execution session plan in `finalize`.

## Advance

Call `next_step()`.
