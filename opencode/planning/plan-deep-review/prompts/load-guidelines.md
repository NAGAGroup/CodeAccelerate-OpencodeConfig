# Node: load-guidelines — /plan-deep-review

The plan design guidelines have been loaded into context from `~/.config/opencode/planning/plan-design-guidelines.md`.

Internalize the schema requirements and planning best-practices before proceeding. You will need them when writing the session plan in `finalize`.

## Advance

**Call `next_step()`** to advance.
