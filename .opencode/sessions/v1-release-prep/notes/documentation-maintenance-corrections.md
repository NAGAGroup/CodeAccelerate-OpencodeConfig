# Note: DOCUMENTATION_MAINTENANCE.md corrections

## What was fixed
Removed all references to `opencode/commands/README.md` — a file that does not exist and was never created.

## What was removed
- The entire "Workflow & Agent Reference Documentation" section (heading + table with one row)
- Step 1 in the "Adding a New Workflow" code block (instructed updating the non-existent README.md)
- A "No longer needed" comment in step 2 of that same block
- A trailing `Note:` line at the bottom of the block referencing `opencode/commands/` as a dynamic source

## Renumbering
Steps in the "Adding a New Workflow" block renumbered from (2, 3, 4) → (1, 2, 3).

## No count changes needed
The `(4 Files)` label in "Core User Documentation" accurately reflects the 4 rows that remain.

## Date
2026-03-15
