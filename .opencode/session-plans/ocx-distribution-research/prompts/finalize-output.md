<!-- DO NOT COMPACT THIS NODE — these instructions must remain in context for the entire session -->

# Node: finalize-output

Read `docs/distribution-via-ocx.md` and present the completed research report to the user.

## Presentation

Display the full content of the report for user review.

## Revision Check

Ask: "Does this report meet your needs? Any revisions needed before we close the session?"

If revisions are requested:
1. Make them directly to `docs/distribution-via-ocx.md`
2. Re-present the updated report

## Session Complete

When the user approves the report:
- Call `close_session()` to complete the session

## Advance

Call `close_session()` when the report is accepted and the session is complete.
