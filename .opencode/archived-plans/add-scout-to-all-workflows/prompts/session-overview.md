<!-- DO NOT COMPACT THIS NODE — these instructions must remain in context for the entire session -->

# Session Overview — Add Scout to All Planning Workflows

## Goal

Add/reposition a scout context-gathering node directly after `load-guidelines` in all 5 planning workflows, so planning agents have codebase awareness before asking clarifying questions.

## What This Session Is

5 scoped configuration edits across 5 plan.json files and 3 new prompt files under `~/.config/opencode/planning/`. No loops, no gates. All subtasks are independent and can run in parallel.

## Output Artifacts

Session files live at: `.opencode/session-plans/add-scout-to-all-workflows/`

## Operating Instructions

5 subtask prompts are agent-internal. Execute them in order (they run in parallel via JuniorDev agents). Do not skip any.

## Advance

Read this overview once, internalize it, then call `next_step()` immediately.
